# Dev/test dependencies:
#   pip install -r requirements-dev.txt && pytest
#
# The suite needs no environment -- no SUPABASE_URL, no keys, no network. It
# does need the runtime deps importable, because testing a store method means
# importing services/supabase_service.py, which imports `supabase` at module
# scope. Pulling in requirements.txt rather than re-pinning a subset here, so
# the two cannot drift.
-r requirements.txt

pytest==7.4.4

# fastapi.testclient needs httpx. It is already installed transitively (via
# supabase), but the suite imports it directly now -- declared here so a
# dependency bump that drops it fails at install rather than at import.
httpx==0.27.2
